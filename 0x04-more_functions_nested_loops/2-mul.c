#include "main.h"

/**
 * mul - Entry point
 * @a: the integer to work with
 * @b: the integer to work with
 * Return: 0 for uppercase and 1 for lowercase
 */

int mul(int a, int b)
{
return (a * b);
}
